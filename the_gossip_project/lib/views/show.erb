<!DOCTYPE html>
<html>
<head>
    <title>The Sinatra Project - Citation</title>
<link rel="stylesheet" type="text/css" href="<%= url("/css/style.css")%>">
</head>
  <body>
    <header>
    <a href="/"><img src="/images/header.png" alt="Sacré_Hubert" height="100" width="100"></a>
    <h1>P*tin de Sinatra !</h1>
    </header>
    <h2>Voici la page du p*tin n°<%= id %></h2>
      <p>Auteur : <%= gossip.author %></p>
      <p>P*tin: <%= gossip.content %></p>
      <p class="bouton"><a href="/gossips/<%= id.to_i %>/edit/">Editer le p*tin</a></p>
    <hr>
    <h3>Commentaires sur cette p*tin:</h3>
    <% allcomment.each do |comment| %>
      <p>
        <% if comment.id == id %>
          <blockquote>
            <span><%= comment.content %></span>
          </blockquote>
        <% end %>
      </p>
    <% end %>
    <form class="commentaire" action='/gossips/<%= id %>' method='POST'>
      Balance ton commentaire :  <input class="comminput" type='text' name='comm_content'/><br/>
      <input type='submit'/>
    </form>
    <hr>
    <p class="bouton"><a href="/">Retour accueil</a></p>
  </body>
</html>
